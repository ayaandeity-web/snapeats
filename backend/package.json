{
  "name": "snapeats-backend",
  "version": "1.0.0",
  "description": "Backend proxy for SnapEats — holds the Groq API key and forwards nutrition analysis requests.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
